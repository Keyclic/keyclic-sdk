/**
 * Keyclic API Reference
 * The Keyclic API is build around [REST](https://en.wikipedia.org/wiki/Representational_State_Transfer). <br/> Our API has resource oriented URLs and uses HTTP response codes to indicate API errors. We support CQRS and all API accesses are performed over HTTPS, allowing you to interact securely with our API from a client-side web application.<br/><br/> **Authentication** <br/> Authentication is performed thought [JSON Web Tokens](https://jwt.io). <br/> JSON Web Tokens are an open industry standard RFC 7519 method for representing claims securely between two parties.<br/>There are many [open source projects](https://auth0.com/opensource) built around JWT in several programming languages that you can use in your application.<br/><br/>**Errors** <br/>We uses conventional HTTP response codes to indicate the success or failure of an API request, errors are represented as JSON objects following the [vnd.error](https://github.com/blongden/vnd.error) standard.<br/><br/>**Integration**<br/>To make the API integration easy as possible we have a sandbox environment to interact securely with our API.
 *
 * OpenAPI spec version: 1.12.1
 * Contact: techies@keyclic.com
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import Error from '../model/Error';
import Feedback from '../model/Feedback';
import FeedbackBody from '../model/FeedbackBody';
import FeedbackPagination from '../model/FeedbackPagination';
import FeedbackStatePatchDocument from '../model/FeedbackStatePatchDocument';

/**
 * Feedback service.
 * @module api/FeedbackApi
 * @version 1.12.1
 */
export default class FeedbackApi extends ApiClient {
    /**
     * Constructs a new FeedbackApi.
     * 
     * @alias module:api/FeedbackApi
     * @class
     * @param {module:ApiClient} apiClient Optional API client implementation to use,
     * default to {@link module:ApiClient#instance} if unspecified.
     */
    constructor(
        basePath = null,
        headers = null,
        timeout = null
    ) {
        super(basePath, headers, timeout);
    }


    /**
     * Retrieve all feedbacks for the given application.
     * @param {Object} credentials The required credentials with good properties to use different types of authentication.
     * @param {FeedbackPagination} returnType The required type to return; can be a string for simple types or the constructor for a complex type.
     * @param {Number} page The page number of the overview.  (default to 1)
     * @param {Number} limit The number of items per page.  (default to 20)
     * @param {Date} before Filter by the creation date. 
     * @param {Date} after Filter by the creation date. 
     * @param {String} geoNear Filter by a radius and a distance. 
     * @param {String} geoHash Filter by a geohash. 
     * @param {String} organization Filter by an organization. 
     * @param {String} category Filter by a category. 
     */
    cget(
        credentials,
        returnType = FeedbackPagination,
        page = 1,
        limit = 20,
        before = null,
        after = null,
        geoNear = null,
        geoHash = null,
        organization = null,
        category = null,
    ) {

        if (typeof credentials === 'undefined' || credentials === null) {
            throw new Error('Missing the required parameter "credentials" when calling cget');
        }

        let pathParams = {
        };

        let bodyParam = null;

        let queryParams = {
            'page': page,
            'limit': limit,
            'before': before,
            'after': after,
            'geo_near': geoNear,
            'geo_hash': geoHash,
            'organization': organization,
            'category': category,
        };

        let headerParams = {
        };

        let formParams = {
        };

        let credentialParams = credentials;

        let authNames = [
            'JWT',
        ];

        let contentTypes = [
            'application/json',
        ];

        let accepts = [
            'application/json',
        ];

        return this.callApi(
            '/feedbacks',
            'GET',
            pathParams,
            queryParams,
            headerParams,
            formParams,
            bodyParam,
            authNames,
            credentialParams,
            contentTypes,
            accepts,
            returnType
        );
    }

    /**
     * Retrieve all feedbacks for the given person.
     * @param {String} person The person id.
     * @param {Object} credentials The required credentials with good properties to use different types of authentication.
     * @param {FeedbackPagination} returnType The required type to return; can be a string for simple types or the constructor for a complex type.
     * @param {Number} page The page number of the overview.  (default to 1)
     * @param {Number} limit The number of items per page.  (default to 20)
     * @param {String} before  
     * @param {String} after  
     */
    cgetByPerson(
        person,
        credentials,
        returnType = FeedbackPagination,
        page = 1,
        limit = 20,
        before = null,
        after = null,
    ) {
        // verify the required parameter 'person' is set
        if (typeof person === 'undefined' || person === null) {
            throw new Error('Missing the required parameter "person" when calling cgetByPerson');
        }

        if (typeof credentials === 'undefined' || credentials === null) {
            throw new Error('Missing the required parameter "credentials" when calling cgetByPerson');
        }

        let pathParams = {
            'person': person,
        };

        let bodyParam = null;

        let queryParams = {
            'page': page,
            'limit': limit,
            'before': before,
            'after': after,
        };

        let headerParams = {
        };

        let formParams = {
        };

        let credentialParams = credentials;

        let authNames = [
            'JWT',
        ];

        let contentTypes = [
            'application/json',
        ];

        let accepts = [
            'application/json',
        ];

        return this.callApi(
            '/people/{person}/feedbacks',
            'GET',
            pathParams,
            queryParams,
            headerParams,
            formParams,
            bodyParam,
            authNames,
            credentialParams,
            contentTypes,
            accepts,
            returnType
        );
    }

    /**
     * Retrieve a feedback.
     * @param {String} feedback 
     * @param {Object} credentials The required credentials with good properties to use different types of authentication.
     * @param {Feedback} returnType The required type to return; can be a string for simple types or the constructor for a complex type.
     */
    get(
        feedback,
        credentials,
        returnType = Feedback,
    ) {
        // verify the required parameter 'feedback' is set
        if (typeof feedback === 'undefined' || feedback === null) {
            throw new Error('Missing the required parameter "feedback" when calling get');
        }

        if (typeof credentials === 'undefined' || credentials === null) {
            throw new Error('Missing the required parameter "credentials" when calling get');
        }

        let pathParams = {
            'feedback': feedback,
        };

        let bodyParam = null;

        let queryParams = {
        };

        let headerParams = {
        };

        let formParams = {
        };

        let credentialParams = credentials;

        let authNames = [
            'JWT',
        ];

        let contentTypes = [
            'application/json',
        ];

        let accepts = [
            'application/json',
        ];

        return this.callApi(
            '/feedbacks/{feedback}',
            'GET',
            pathParams,
            queryParams,
            headerParams,
            formParams,
            bodyParam,
            authNames,
            credentialParams,
            contentTypes,
            accepts,
            returnType
        );
    }

    /**
     * Apply a state transition on the given feedback.
     * Apply transition for a feedback.  For more informations about PATCH implementation, please refer to the RFC 5789.  • https://tools.ietf.org/html/rfc5789  Required transition must be send as a JSON patch document.
     * @param {String} feedback 
     * @param {Array.<module:model/FeedbackStatePatchDocument>} body A JSON document according the rfc5789 specification.
     * @param {Object} credentials The required credentials with good properties to use different types of authentication.
     * @param {Feedback} returnType The required type to return; can be a string for simple types or the constructor for a complex type.
     */
    patchState(
        feedback,
        body,
        credentials,
        returnType = Feedback,
    ) {
        // verify the required parameter 'feedback' is set
        if (typeof feedback === 'undefined' || feedback === null) {
            throw new Error('Missing the required parameter "feedback" when calling patchState');
        }
        // verify the required parameter 'body' is set
        if (typeof body === 'undefined' || body === null) {
            throw new Error('Missing the required parameter "body" when calling patchState');
        }

        if (typeof credentials === 'undefined' || credentials === null) {
            throw new Error('Missing the required parameter "credentials" when calling patchState');
        }

        let pathParams = {
            'feedback': feedback,
        };

        let bodyParam = body;

        let queryParams = {
        };

        let headerParams = {
        };

        let formParams = {
        };

        let credentialParams = credentials;

        let authNames = [
            'JWT',
        ];

        let contentTypes = [
            'application/json',
        ];

        let accepts = [
            'application/json',
        ];

        return this.callApi(
            '/feedbacks/{feedback}/state',
            'PATCH',
            pathParams,
            queryParams,
            headerParams,
            formParams,
            bodyParam,
            authNames,
            credentialParams,
            contentTypes,
            accepts,
            returnType
        );
    }

    /**
     * Add a new image on the given feedback.
     * @param {String} feedback The feedback guid.
     * @param {String} image The image as a string (encoded in base64).
     * @param {Object} credentials The required credentials with good properties to use different types of authentication.
     * @param {Feedback} returnType The required type to return; can be a string for simple types or the constructor for a complex type.
     */
    postImage(
        feedback,
        image,
        credentials,
        returnType = Feedback,
    ) {
        // verify the required parameter 'feedback' is set
        if (typeof feedback === 'undefined' || feedback === null) {
            throw new Error('Missing the required parameter "feedback" when calling postImage');
        }
        // verify the required parameter 'image' is set
        if (typeof image === 'undefined' || image === null) {
            throw new Error('Missing the required parameter "image" when calling postImage');
        }

        if (typeof credentials === 'undefined' || credentials === null) {
            throw new Error('Missing the required parameter "credentials" when calling postImage');
        }

        let pathParams = {
            'feedback': feedback,
        };

        let bodyParam = image;

        let queryParams = {
        };

        let headerParams = {
        };

        let formParams = {
        };

        let credentialParams = credentials;

        let authNames = [
            'JWT',
        ];

        let contentTypes = [
            'application/json',
        ];

        let accepts = [
            'application/json',
        ];

        return this.callApi(
            '/feedbacks/{feedback}/images',
            'POST',
            pathParams,
            queryParams,
            headerParams,
            formParams,
            bodyParam,
            authNames,
            credentialParams,
            contentTypes,
            accepts,
            returnType
        );
    }

    /**
     * Create a new feedback (type issue).
     * @param {module:model/FeedbackBody} body 
     * @param {Object} credentials The required credentials with good properties to use different types of authentication.
     * @param {Feedback} returnType The required type to return; can be a string for simple types or the constructor for a complex type.
     */
    postIssue(
        body,
        credentials,
        returnType = Feedback,
    ) {
        // verify the required parameter 'body' is set
        if (typeof body === 'undefined' || body === null) {
            throw new Error('Missing the required parameter "body" when calling postIssue');
        }

        if (typeof credentials === 'undefined' || credentials === null) {
            throw new Error('Missing the required parameter "credentials" when calling postIssue');
        }

        let pathParams = {
        };

        let bodyParam = body;

        let queryParams = {
        };

        let headerParams = {
        };

        let formParams = {
        };

        let credentialParams = credentials;

        let authNames = [
            'JWT',
        ];

        let contentTypes = [
            'application/json',
        ];

        let accepts = [
            'application/json',
        ];

        return this.callApi(
            '/feedbacks/issues',
            'POST',
            pathParams,
            queryParams,
            headerParams,
            formParams,
            bodyParam,
            authNames,
            credentialParams,
            contentTypes,
            accepts,
            returnType
        );
    }

}
