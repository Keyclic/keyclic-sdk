/**
 * Keyclic API Reference
 * The Keyclic API is build around [REST](https://en.wikipedia.org/wiki/Representational_State_Transfer). <br/> Our API has resource oriented URLs and uses HTTP response codes to indicate API errors. We support CQRS and all API accesses are performed over HTTPS, allowing you to interact securely with our API from a client-side web application.<br/><br/> **Authentication** <br/> Authentication is performed thought [JSON Web Tokens](https://jwt.io). <br/> JSON Web Tokens are an open industry standard RFC 7519 method for representing claims securely between two parties.<br/>There are many [open source projects](https://auth0.com/opensource) built around JWT in several programming languages that you can use in your application.<br/><br/>**Errors** <br/>We uses conventional HTTP response codes to indicate the success or failure of an API request, errors are represented as JSON objects following the [vnd.error](https://github.com/blongden/vnd.error) standard.<br/><br/>**Integration**<br/>To make the API integration easy as possible we have a sandbox environment to interact securely with our API.
 *
 * OpenAPI spec version: 1.12.1
 * Contact: techies@keyclic.com
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import Category from '../model/Category';
import CategoryBody from '../model/CategoryBody';
import CategoryPagination from '../model/CategoryPagination';
import CategoryPatchDocument from '../model/CategoryPatchDocument';
import Error from '../model/Error';

/**
 * Category service.
 * @module api/CategoryApi
 * @version 1.12.1
 */
export default class CategoryApi extends ApiClient {
    /**
     * Constructs a new CategoryApi.
     * 
     * @alias module:api/CategoryApi
     * @class
     * @param {module:ApiClient} apiClient Optional API client implementation to use,
     * default to {@link module:ApiClient#instance} if unspecified.
     */
    constructor(
        basePath = null,
        headers = null,
        timeout = null
    ) {
        super(basePath, headers, timeout);
    }


    /**
     * Retrieve all categories available for the given application.
     * The list of the categories available to use them when your are creating a new feedback can be retrieve using this endpoint.  A valid access token, application token and geoCoordinates parameter are required to retrieve categories which are located in the places which are matching the given geoCoordinates.  Application token must be stored in the request header \&quot;X-Keyclic-App\&quot;.
     * @param {String} xKeyclicApp The application token.
     * @param {Object} credentials The required credentials with good properties to use different types of authentication.
     * @param {CategoryPagination} returnType The required type to return; can be a string for simple types or the constructor for a complex type.
     * @param {Number} page The page number of the overview.  (default to 1)
     * @param {Number} limit The number of items per page.  (default to 20)
     * @param {String} geoCoordinates Filter by geo coordinates. 
     * @param {String} organization Filter by an organization. 
     */
    cget(
        xKeyclicApp,
        credentials,
        returnType = CategoryPagination,
        page = 1,
        limit = 20,
        geoCoordinates = null,
        organization = null,
    ) {
        // verify the required parameter 'xKeyclicApp' is set
        if (typeof xKeyclicApp === 'undefined' || xKeyclicApp === null) {
            throw new Error('Missing the required parameter "xKeyclicApp" when calling cget');
        }

        if (typeof credentials === 'undefined' || credentials === null) {
            throw new Error('Missing the required parameter "credentials" when calling cget');
        }

        let pathParams = {
        };

        let bodyParam = null;

        let queryParams = {
            'page': page,
            'limit': limit,
            'geo_coordinates': geoCoordinates,
            'organization': organization,
        };

        let headerParams = {
            'x-keyclic-app': xKeyclicApp,
        };

        let formParams = {
        };

        let credentialParams = credentials;

        let authNames = [
            'JWT',
        ];

        let contentTypes = [
            'application/json',
        ];

        let accepts = [
            'application/json',
        ];

        return this.callApi(
            '/categories',
            'GET',
            pathParams,
            queryParams,
            headerParams,
            formParams,
            bodyParam,
            authNames,
            credentialParams,
            contentTypes,
            accepts,
            returnType
        );
    }

    /**
     * Retrieve all categories for the given organization.
     * @param {String} organization 
     * @param {Object} credentials The required credentials with good properties to use different types of authentication.
     * @param {CategoryPagination} returnType The required type to return; can be a string for simple types or the constructor for a complex type.
     * @param {Number} page The page number of the overview.  (default to 1)
     * @param {Number} limit The number of items per page.  (default to 20)
     * @param {String} search  
     */
    cgetByOrganization(
        organization,
        credentials,
        returnType = CategoryPagination,
        page = 1,
        limit = 20,
        search = null,
    ) {
        // verify the required parameter 'organization' is set
        if (typeof organization === 'undefined' || organization === null) {
            throw new Error('Missing the required parameter "organization" when calling cgetByOrganization');
        }

        if (typeof credentials === 'undefined' || credentials === null) {
            throw new Error('Missing the required parameter "credentials" when calling cgetByOrganization');
        }

        let pathParams = {
            'organization': organization,
        };

        let bodyParam = null;

        let queryParams = {
            'page': page,
            'limit': limit,
            'search': search,
        };

        let headerParams = {
        };

        let formParams = {
        };

        let credentialParams = credentials;

        let authNames = [
            'JWT',
        ];

        let contentTypes = [
            'application/json',
        ];

        let accepts = [
            'application/json',
        ];

        return this.callApi(
            '/organizations/{organization}/categories',
            'GET',
            pathParams,
            queryParams,
            headerParams,
            formParams,
            bodyParam,
            authNames,
            credentialParams,
            contentTypes,
            accepts,
            returnType
        );
    }

    /**
     * Retrieve a category.
     * @param {String} category 
     * @param {Object} credentials The required credentials with good properties to use different types of authentication.
     * @param {Category} returnType The required type to return; can be a string for simple types or the constructor for a complex type.
     */
    get(
        category,
        credentials,
        returnType = Category,
    ) {
        // verify the required parameter 'category' is set
        if (typeof category === 'undefined' || category === null) {
            throw new Error('Missing the required parameter "category" when calling get');
        }

        if (typeof credentials === 'undefined' || credentials === null) {
            throw new Error('Missing the required parameter "credentials" when calling get');
        }

        let pathParams = {
            'category': category,
        };

        let bodyParam = null;

        let queryParams = {
        };

        let headerParams = {
        };

        let formParams = {
        };

        let credentialParams = credentials;

        let authNames = [
            'JWT',
        ];

        let contentTypes = [
            'application/json',
        ];

        let accepts = [
            'application/json',
        ];

        return this.callApi(
            '/categories/{category}',
            'GET',
            pathParams,
            queryParams,
            headerParams,
            formParams,
            bodyParam,
            authNames,
            credentialParams,
            contentTypes,
            accepts,
            returnType
        );
    }

    /**
     * Edit a category.
     * Update one category resource.  For more information about PATCH implementation, please refer to the RFC.  • https://tools.ietf.org/html/rfc5789
     * @param {String} category 
     * @param {Array.<module:model/CategoryPatchDocument>} body A JSON document according the rfc5789 specification.
     * @param {Category} returnType The required type to return; can be a string for simple types or the constructor for a complex type.
     */
    patch(
        category,
        body,
        returnType = Category,
    ) {
        // verify the required parameter 'category' is set
        if (typeof category === 'undefined' || category === null) {
            throw new Error('Missing the required parameter "category" when calling patch');
        }
        // verify the required parameter 'body' is set
        if (typeof body === 'undefined' || body === null) {
            throw new Error('Missing the required parameter "body" when calling patch');
        }


        let pathParams = {
            'category': category,
        };

        let bodyParam = body;

        let queryParams = {
        };

        let headerParams = {
        };

        let formParams = {
        };

        let credentialParams = null;

        let authNames = [
        ];

        let contentTypes = [
            'application/json',
        ];

        let accepts = [
            'application/json',
        ];

        return this.callApi(
            '/categories/{category}',
            'PATCH',
            pathParams,
            queryParams,
            headerParams,
            formParams,
            bodyParam,
            authNames,
            credentialParams,
            contentTypes,
            accepts,
            returnType
        );
    }

    /**
     * Create a new category for the given organization.
     * @param {String} organization The name id of the organization.
     * @param {module:model/CategoryBody} body 
     * @param {Object} credentials The required credentials with good properties to use different types of authentication.
     * @param {Category} returnType The required type to return; can be a string for simple types or the constructor for a complex type.
     */
    postByOrganization(
        organization,
        body,
        credentials,
        returnType = Category,
    ) {
        // verify the required parameter 'organization' is set
        if (typeof organization === 'undefined' || organization === null) {
            throw new Error('Missing the required parameter "organization" when calling postByOrganization');
        }
        // verify the required parameter 'body' is set
        if (typeof body === 'undefined' || body === null) {
            throw new Error('Missing the required parameter "body" when calling postByOrganization');
        }

        if (typeof credentials === 'undefined' || credentials === null) {
            throw new Error('Missing the required parameter "credentials" when calling postByOrganization');
        }

        let pathParams = {
            'organization': organization,
        };

        let bodyParam = body;

        let queryParams = {
        };

        let headerParams = {
        };

        let formParams = {
        };

        let credentialParams = credentials;

        let authNames = [
            'JWT',
        ];

        let contentTypes = [
            'application/json',
        ];

        let accepts = [
            'application/json',
        ];

        return this.callApi(
            '/organizations/{organization}/categories',
            'POST',
            pathParams,
            queryParams,
            headerParams,
            formParams,
            bodyParam,
            authNames,
            credentialParams,
            contentTypes,
            accepts,
            returnType
        );
    }

}
